# NeuroForge Nexus — frontend environment template
# Copy this file to frontend/.env and adjust per environment.
# Never commit real secrets — .env is git-ignored, this template is not.
VITE_API_URL=http://localhost:8081
